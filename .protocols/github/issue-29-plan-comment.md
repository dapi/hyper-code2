The governed R-029 plan is now complete and approved for static route/authority
inventory plus mocked `Bun.serve` and direct-handler checks only.

No real listener or socket may be opened. Real reachability remains separately
unapproved and would require a disposable isolated network with no public,
shared or user-local route. The collection must reconcile route files with loader
output, classify direct/transitive authority and caller controls, and preserve
commit-pinned evidence without choosing an authentication or isolation mechanism.
