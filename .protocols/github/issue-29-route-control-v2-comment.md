R-029 technical collection and independent review are complete for the bounded
committed-`src` caller-control claim.

The additive [`2026-08-13-d119f1c-route-control-v2` carrier](https://github.com/dapi/hyper-code2/tree/chore/memory-bank-adoption/.protocols/experiments/runs/R-029/2026-08-13-d119f1c-route-control-v2)
does not replace the first static/mock run. It reconciles all 36
committed-source dispatch entries against the fixed `d119f1c` source baseline
and records, for every entry:

- common-dispatch and route-local caller-control evidence;
- immediate and deferred paths across all nine authority classes;
- three captured dispatcher variants (missing, malformed and synthetic-invalid
  identity material), for 108/108 handler-boundary calls;
- an existing repository-test reference or an explicit static-only rationale.

The run opened no socket, used no real secret/home/provider/shared state,
executed no real handler side effect, intercepted runtime writes and verified
all five JSON carrier checksums.

[R-029](https://github.com/dapi/hyper-code2/tree/chore/memory-bank-adoption/memory-bank/research/R-029)
is terminal `validated` for the bounded committed-`src` gap. A separate reviewer signed off 36 unique entries, exact
source hashes, 108 dispatcher cases, seven test-linked plus 29 static-only
classifications, shared dispatch sources, two lexical false positives, nine
authority classes and all carrier checksums. Static-only evidence was accepted
for source-level caller-control absence, not for functional handler behavior.

No mechanism is selected. `.hyper` runtime overlays, external middleware,
manual authority labels as runtime behavior and real interface reachability
remain outside the signed-off claim.
