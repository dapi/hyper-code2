The governed R-018 plan is now complete and approved for static inventory plus an
isolated synthetic-sentinel matrix only. Real secrets, provider calls, public or
shared network access, home/credential reads and production state are prohibited.

The plan predeclares source/sink coverage, success/error/serialization paths,
finite sentinel encodings S0–S8, positive/negative controls, stable sanitized
evidence carriers and stop rules. Collection does not select opaque handles,
projection, redaction or another mechanism. GitHub #19 remains parked.
