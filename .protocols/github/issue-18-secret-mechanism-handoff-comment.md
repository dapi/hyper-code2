The mechanism-neutral handoff `HD-02` is now routed separately to #33.

R-018 remains terminal `validated` only for non-conformance of the sampled
current route. #33 compares opaque broker/references, source exclusion, bounded
projection, provenance-aware redaction and combinations against one symmetric
static/mock/prototype matrix. It does not reopen R-018 or select a mechanism.

Real secrets, credential-store reads, provider/network calls, shared state and
production implementation remain prohibited.
