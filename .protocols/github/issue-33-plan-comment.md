The governed R-033 package is prepared locally in `memory-bank/research/R-033/`
with lifecycle state `collecting`.

The approved plan defines:

- candidates `CAND-01…05`: opaque broker/reference, source exclusion, bounded
  projection, provenance-aware redaction and a specified combination;
- common cells `CC-01…12` covering controls, four current adapter shapes,
  unknown/revoked/unavailable resolution, schema/classification failure,
  action outcomes, retry/cancellation and restart;
- identical prohibited-sink assertions, compatibility/composition criteria and
  fail-closed rules for every candidate;
- an independent inventory review gate before prototype injection and another
  carrier/symmetry review before synthesis;
- the ADR trigger and explicit rule that #19 remains parked.

Only read-only static inventory has run so far. No candidate prototype, real
secret, credential-store read, provider/network call, listener, shared state or
production implementation has run. Candidate collection waits at the independent
inventory-review gate.
