# R-029 static/mock run 2026-08-13-d119f1c-static-mock

Fixed source snapshot: `d119f1c10381e489d9140c43f9fe9a878bf67255`. The repository worktree was dirty outside `src/`; `provenance.json` preserves that limitation. No socket was opened. All privileged roots used by direct-handler checks were stubs. The run used synthetic requests/state and did not read real environment values, home files, credentials or shared runtime state.

- `route-authority-inventory.json`: 36 reconciled loader entries.
- `mock-results.json`: 11 synthetic cases.
- `SHA256SUMS`: checksums for the JSON carriers.
