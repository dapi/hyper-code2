# R-033 rejected semantic-adapter attempt 2026-08-13-semantic-adapters-v3

This carrier is rejected as candidate evidence. Independent review found that
the five candidates still shared one resolver with trace-label differences,
so it did not execute genuinely distinct broker binding, source exclusion,
schema projection, provenance redaction and composed-control mechanisms. CC-05
and CC-06 refresh operations did not traverse distinct candidate mediation;
CC-13 reused the CC-06 request rather than a separate discovery contract;
S0-S8 layer controls ran only in the parent detector; and CC-10 did not prove a
secret-bearing cyclic serialization failure before a durable path. Instrument
code changed after this carrier, so its recorded provenance no longer matches
the worktree either.

The carrier is retained only as a rejected review trace. Its candidate totals,
semantic-coverage flags and failures cannot enter synthesis. R-033 remains
`collecting`; no mechanism, winner or ADR is selected.
