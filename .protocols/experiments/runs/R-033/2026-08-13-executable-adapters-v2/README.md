# R-033 executable disposable adapters 2026-08-13-executable-adapters-v2

This additive sanitized carrier proves bounded integrity, containment, schema and
partial adapter execution only. Its methods execute synthetic resolution,
authentication placement, basic projection/redaction, failure, retry-state,
restart-state and direct-authority fixtures; outcomes derive from that state
rather than parent candidate constants. Independent review found that several
CC cells still lacked their full planned semantics, per-layer transformed leak
controls and independent full-request controls. Therefore its candidate totals
are partial instrument output, not complete CC conformance evidence.

The child runs under a deny-default macOS profile with a six-key non-auth
environment. It uses no real credential/store, provider call, listener, shared
state or production source. It neither establishes complete candidate fidelity
nor selects or ranks a candidate. R-033 remains collecting and synthesis is
blocked.

- `results.json`: sanitized executed outcomes, traces and per-cell checksums.
- `controls.json`: frozen expected auth digests and 70 injected-leak detector controls.
- `summary.json`: matrix accounting and failures derived from execution.
- `provenance.json` and `SHA256SUMS`: instrument/source identity and integrity.
