# R-033 rejected outcome-model dry run 2026-08-13-contained-symmetry-v1

This sanitized carrier proves only containment, a rotating 70-cell case-plan
schema and carrier accounting. Independent review rejected it as candidate
evidence: the child emitted only the plan, the parent assigned outcomes from
candidate/cell constants, and authentication digests were self-compared. Its
`pass`/`fail` fields and compatibility labels are rejected dry-run outputs, not
observations about candidate mechanisms or representability.

The containment child used a deterministic non-secret fixture boundary and no
credential-bearing environment value, settings/database row, credential-file
content, keychain value, provider, network, listener, shared runtime state or
production source. The parent HOME path was used only to construct deny rules.
This carrier selects no mechanism, winner or ADR and must not enter synthesis.

- `containment.json`: pre-injection environment, filesystem, network/listener and keychain denial controls.
- `controls.json`: sanitized detector and privileged-transport positive/negative controls.
- `results.json`: rejected parent-generated outcome assertions, retained for review trace.
- `compatibility.json`: plan-time labels only; not candidate evidence.
- `summary.json`: case accounting plus rejected output totals.
- `provenance.json` and `SHA256SUMS`: source/instrument identity and carrier integrity.
